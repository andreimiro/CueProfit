import { PlaceholderPage } from "@/components/app/placeholder-page";

export default function ProductsPage() {
  return (
    <PlaceholderPage
      title="Products"
      subtitle="Margin truth per SKU — find the products that lose money after costs"
      icon="products"
      emptyTitle="Connect a source to see products"
      emptyDescription="Add product costs and connect Merchant Center to reveal per-product profit, break-even ROAS and margin pressure."
    />
  );
}
