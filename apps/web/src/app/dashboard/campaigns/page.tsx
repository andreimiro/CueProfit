import { PlaceholderPage } from "@/components/app/placeholder-page";

export default function CampaignsPage() {
  return (
    <PlaceholderPage
      title="Campaigns"
      subtitle="True profit, POAS and the next action for every campaign"
      icon="campaigns"
      emptyTitle="Connect Google Ads to see campaigns"
      emptyDescription="We import your campaigns and rank each one by net profit — so you can scale winners and cap the losers."
    />
  );
}
